# 开发计划

## 1. 开发阶段（功能实现）

### 1.1 聊天

- **妙讯对话框**
- **查询功能**：查询对话记录/Agent（具体形式可以参考微信）、通讯录（参考微信，点击后打开新的对话框）
- **妙管家**：能够接入通用AI模型，先完成正常的问答，交互方式参考原型
- **每个对话框中的"右划交互"**：右滑后内容先显示空白即可，左滑返回聊天框，后续增加"妙动"按钮，后续输出该按钮设计。
- **左上角点击后的"创建群"功能暂缓**，扫码功能打开摄像头即可，后续需要建立扫码和跳转的规范。
- **聊天、任务的未读消息**

### 1.2 小站

- **生成AI ID**：先采用6位排序数字+手机号后6位的方式；邮箱注册时后6位采用邮箱规范化后的稳定数字摘要，AI ID 保持纯数字且注册后不变
- **设置**：可见范围包含（全平台、平台内所有人、粉丝、好友、分组、仅自己）
- **通知设置**：常规
- **我的文件**：主要用于存储平台内文件（包括图片、文件等，这些文件能够通过妙管家Agent和类似文件管理Agent检索、调用）、用户所有数据（用于强调用户数据所有权）

---

## 2. 功能类Agent（AI应用）[测试，优先测试]{.underline}

- **生成3D模型**：自然语言生成3D形象，后续能够选择配饰装饰，能够由用户自定义导入不同的数据源和生成频率，**首期完成通过图片，以及用户提出需求，Agent自主在相册中自动找到符合的照片自动生成3D模型**。
- **AI建站Agent**：自然语言生成个人小站布局，最好由AI进行引导创建，后续可提供模板供用户选择，但自然语言建站的能力需要存在。**生成布局->定义每个板块->进入对应板块的Agent提出要求，该流程由Agent通过问答的方式引导进行，也可以直接推荐模板。**
- **文件预处理Agent**：能够将用户上传的图片、视频、文件、应用等文件形式，预处理成为AI能够阅读的形式，以便于后续检索和使用。
- **漫画日记**：能够通过用户口述今天的记忆或者由AI总结用户当天的行为与对话，用四格漫画的形式生成每日日记，用户能够对漫画的风格进行自定义。**目前实现通过小作文生成多格漫画。**
- **相册管理**：支持用户上传任意数量的照片作为网络相册，用户能够通过文字对照片自动选取、自动修改，以及自动生成动态。基于上面的文件预处理Agent，**用户能够批量对满足某些条件的照片进行修改，例如美容、调色、裁切等。**
- **视频制作**：通过自然语言生成视频，Agent应当能自动调取用户画像中必要的信息，例如用户的形象、场景喜好、风格等。目前实现通过自然语言，并导入预制的风格喜好、照片等生成5~10s的短片。

---

## 3. 系统级Agent（AI应用）[设计规划好 再执行！]{.underline}

### 3.1 Agent结构

![Agent结构示意图](media/image1.png)

- **妙管家**：定位为最高权限的管家级Agent，定位与之前的跃目.AG一致，承担调度平台内的Agent，对系统进行设置，对用户的权力进行保障，对用户的需求进行广泛的处理。
- **用户画像Agent**：能够搜集平台内的聊天、主动提供的信息、浏览情况等，动态生成更高信息密度的用户画像，包含基础数据、缓慢动态数据、实时数据、场景偏好、内容偏好等等，**至于图像等形式的内容以文本还是图像存储需要商榷。**
- **安全助手**：若Agent或用户在沟通牵扯到契约、交付、交易等内容时候，安全助手应当介入，进行监控或者提醒，以规避风险或者作为后续纠纷的依据。

---

## 4. 技术栈

- **主要编程语言**：iOS-Swift，**安卓-?，后端-？**，PC-Web/套壳Web
- **主要数据库选型**：PostgreSQL
- **系统应用端**：iOS移动端 > 安卓移动端 > PC web > PC应用

---

## 5. 技术远景

- **APP->WEB端**：用户能够通过妙讯创造个人站，并支持一键生成web页面，以公司二级域名为地址，生成web页面后，仅供展示，不可以修改，不可以使用AI能力。
- **Agent的开放**：考虑是否能够将平台内收录的Agent开放至互联网中，形式不限于web，探寻将Agent有序、安全的形成在线服务模式。
- **Agent的收录**：考虑是否收录平台外的AI Agent进入平台，需要考虑安全性、利益、资源问题。
- **UI自动化**：目前基于微讯聊天界面中的"右滑"操作，定位为聊天页面中，支持通过自然语言或者动态生成的交互组件进行沟通，右滑后也支持通过传统软件的方式对应用进行操作，是否存在可能性，通过用户画像和用户意图判断，对右滑后的传统软件界面进行个性化自动生成？
