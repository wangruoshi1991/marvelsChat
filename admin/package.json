{
  "name": "@miaoxun/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Miaoxun admin console frontend.",
  "scripts": {
    "check": "node --check *.js test/*.js",
    "test": "node --test test/*.test.js",
    "dev": "vite --host 127.0.0.1",
    "dev:prod-api": "MIAOXUN_ADMIN_API_TARGET=https://api.marvelschat.com vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "overrides": {
    "nanoid": "3.3.18",
    "postcss": "8.5.25"
  },
  "engines": {
    "node": ">=20"
  }
}
