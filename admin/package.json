{
  "name": "@miaoxun/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Miaoxun admin console frontend.",
  "scripts": {
    "check": "node --check admin.js vite.config.js",
    "dev": "vite --host 127.0.0.1",
    "dev:prod-api": "MIAOXUN_ADMIN_API_TARGET=https://api.marvelschat.com vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=20"
  }
}
